import { GameData } from "../types/GameData"; export class SessionHistoryComponent { render(data: GameData) { if (!data || !data.session_history) return "<p>No session history was found.</p>"; const validSessions = data.session_history.filter(session => session !== null); const rows = validSessions.slice(-20).reverse().map(session => `<tr class="session-row"><td class="session-game">${session.game_name}</td><td class="session-start">${session.start_time}</td><td class="session-duration">${session.duration} Min</td></tr>`).join(""); return `<div id="session-history-view"><h2>Recent Session History</h2><table><thead><tr><th>Game</th><th>Start Time</th><th>Duration</th></tr></thead><tbody>${rows}</tbody></table></div>`; } }
